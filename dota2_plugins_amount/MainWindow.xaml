<Window x:Class="Dota2NetWorth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dota2 资产插件" Height="42" Width="140"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp">
    <Border x:Name="BgBorder" CornerRadius="8" Background="Transparent" BorderThickness="0" SnapsToDevicePixels="True">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,10,0">
            <Image Source="icon.png" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality"/>
            <TextBlock Text="{Binding DisplayText, FallbackValue=Ready}"
                       Foreground="#F8E8B9" FontSize="16" FontWeight="Bold"
                       Margin="5,0,0,0" VerticalAlignment="Center" Width="80">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="3" Direction="315" ShadowDepth="2" Color="Black" Opacity="1"/>
                </TextBlock.Effect>
            </TextBlock>
        </StackPanel>
    </Border>
</Window>